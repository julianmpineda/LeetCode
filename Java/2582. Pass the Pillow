class Solution {
    public int passThePillow(int n, int time) {
        int pos = 1, dir = 1;

        for (int i = 0; i < time; i++) {
            pos += (1 * dir);

            if (pos == 1 || pos == n) {
                dir *= -1;
            }
        }

        return pos;
    }
}

class Solution {
    public String getEncryptedString(String s, int k) {
        StringBuilder encrypted = new StringBuilder();

        for (int i = 0; i < s.length(); i++) {
            encrypted.append(s.charAt((i + k) % s.length()));
        }

        return encrypted.toString();
    }
}

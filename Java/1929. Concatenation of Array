class Solution {
    public int[] getConcatenation(int[] nums) {
        int len = nums.length;
        int[] concat = new int[len*2];

        for (int i = 0; i < len; i++) {
            concat[i] = nums[i];
            concat[i + len] = nums[i];
        }

        return concat;
    }
}

class Solution {
    public int numberOfEmployeesWhoMetTarget(int[] hours, int target) {
        int employees = 0;

        for (int h : hours) {
            if (h >= target) {
                employees++;
            }
        }

        return employees;
    }
}

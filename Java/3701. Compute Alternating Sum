class Solution {
    public int alternatingSum(int[] nums) {
        int sum = 0, flag = 1;

        for (int i = 0; i < nums.length; i++) {
            sum += nums[i] * flag;
            flag *= -1;
        }

        return sum;
    }
}

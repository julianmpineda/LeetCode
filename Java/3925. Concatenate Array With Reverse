class Solution {
    public int[] concatWithReverse(int[] nums) {
        int[] cat = new int[2*nums.length];

        for (int i = 0; i < nums.length; i++) {
            cat[i] = nums[i];
            cat[cat.length - i - 1] = nums[i];
        }

        return cat;
    }
}

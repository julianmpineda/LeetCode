class Solution {
    public int minimumOperations(int[] nums) {
        int count = 0;

        for (int n : nums) {
            count += Math.min(1, (n % 3));
        }

        return count;
    }
}

class Solution {
    public int[] buildArray(int[] nums) {
        int[] numsNums = new int[nums.length];

        for (int i = 0; i < nums.length; i++) {
            numsNums[i] = nums[nums[i]];
        }

        return numsNums;
    }
}
